import type { Camera } from "./cameras.js";
/**
 * Two-stage Zipstream-inspired pipeline (brand-agnostic RTSP):
 * 1) Remux camera bitstream with -c copy → authentic bytes_in baseline
 * 2) Re-encode with GoV / P/B / mpdecimate / AQ → bytes_out
 * Savings % = (1 - bytes_out/bytes_in) * 100
 */
export declare function compressSegment(camera: Camera): Promise<{
    recordingId: string;
    outputPath: string;
    bytesIn: number;
    bytesOut: number;
    ratio: number;
    profile: string;
    strategy: string;
}>;
export declare function stopCameraJob(cameraId: string): boolean;
export declare function getActiveJobs(): {
    cameraId: string;
    recordingId: string;
}[];
export declare function startCameraLoop(cameraId: string): void;
export declare function stopCameraLoop(cameraId: string): void;
export declare function syncCameraLoops(): void;
export declare function listRecordings(limit?: number): unknown[];
