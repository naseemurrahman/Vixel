import type { FastifyInstance, FastifyRequest } from "fastify";
import { type Role } from "./users.js";
export type JwtUser = {
    sub: string;
    role: Role;
    uid: string;
};
export declare function registerAuth(app: FastifyInstance): Promise<void>;
declare module "@fastify/jwt" {
    interface FastifyJWT {
        payload: JwtUser;
        user: JwtUser;
    }
}
declare module "fastify" {
    interface FastifyInstance {
        authenticate: (request: FastifyRequest, reply: FastifyReply) => Promise<void>;
        requireRole: (minimum: Role) => (request: FastifyRequest, reply: FastifyReply) => Promise<void>;
    }
}
export declare function actorOf(request: FastifyRequest): string;
