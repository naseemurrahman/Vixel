import { config } from "./config.js";
import { roleAtLeast } from "./users.js";
export async function registerAuth(app) {
    await app.register(import("@fastify/jwt"), {
        secret: config.jwtSecret,
        sign: { expiresIn: "12h" },
    });
    app.decorate("authenticate", async (request, reply) => {
        try {
            await request.jwtVerify();
        }
        catch {
            return reply.code(401).send({ error: "Unauthorized" });
        }
    });
    app.decorate("requireRole", (minimum) => {
        return async (request, reply) => {
            try {
                await request.jwtVerify();
            }
            catch {
                return reply.code(401).send({ error: "Unauthorized" });
            }
            const role = request.user?.role;
            if (!role || !roleAtLeast(role, minimum)) {
                return reply.code(403).send({ error: "Forbidden — insufficient role" });
            }
        };
    });
}
export function actorOf(request) {
    return request.user?.sub ?? "anonymous";
}
//# sourceMappingURL=auth.js.map