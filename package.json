{
  "name": "vixel",
  "version": "1.0.0",
  "private": true,
  "description": "Vixel — secure IP camera recording compressor for server deployments",
  "workspaces": [
    "server",
    "web",
    "tools/license-gen"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w server && npm run build -w web",
    "start": "npm run start -w server",
    "license:gen": "npm run start -w tools/license-gen"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
